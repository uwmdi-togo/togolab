% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aws.R
\name{togo_s3_save_plot}
\alias{togo_s3_save_plot}
\title{Save a ggplot (or any object) to the lab S3 store}
\usage{
togo_s3_save_plot(x, object, bucket, FUN = ggplot2::ggsave, region = "", ...)
}
\arguments{
\item{x}{Object to write (e.g. a ggplot).}

\item{object}{Destination object key (including extension).}

\item{bucket}{Bucket name.}

\item{FUN}{Writer function called as \code{FUN(file, x, ...)}. Default \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}

\item{region}{S3 region. Default \code{""} (Kopah).}

\item{...}{Passed to \code{FUN} (e.g. \code{width}, \code{height} for ggsave).}
}
\value{
Invisibly, the result of the upload.
}
\description{
Writes an object to S3 via \code{\link[=s3write_using_region]{s3write_using_region()}} so \code{region} is always
passed (Kopah requires \code{region = ""}). By default saves a ggplot with
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}; the writer is called as \code{FUN(tempfile, x, ...)}, which
matches \code{ggsave(filename, plot, ...)}. For other objects pass a \code{FUN} with
that same \verb{(file, object, ...)} argument order (e.g.
\code{function(file, obj) saveRDS(obj, file)}).
}
\examples{
\dontrun{
togo_s3_save_plot(p, "figures/umap.png", bucket = "scrna", width = 10, height = 10)
}
}
