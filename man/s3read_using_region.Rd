% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aws.R
\name{s3read_using_region}
\alias{s3read_using_region}
\title{Read an S3 object via a reader function, with explicit region}
\usage{
s3read_using_region(
  FUN,
  ...,
  object,
  bucket,
  region = NULL,
  opts = NULL,
  filename = NULL
)
}
\arguments{
\item{FUN}{Reader function applied to the downloaded temp file (e.g.
\code{\link[utils:read.table]{utils::read.csv()}}, \code{\link[base:readRDS]{base::readRDS()}}, \code{qs::qread}).}

\item{...}{Additional arguments passed to \code{FUN}.}

\item{object}{S3 object key (or a full \verb{s3://bucket/key} string, in which
case \code{bucket} may be omitted).}

\item{bucket}{Bucket name. If missing, parsed from \code{object}.}

\item{region}{S3 region. Merged into \code{opts$region} when supplied.}

\item{opts}{Optional list of further arguments passed to
\code{aws.s3::save_object()}.}

\item{filename}{Optional fixed temp filename (otherwise a tempfile with the
object's extension is used).}
}
\value{
The value returned by \code{FUN}.
}
\description{
Downloads an S3 object to a temp file and reads it with \code{FUN}. Like
\code{aws.s3::s3read_using()} but lets you pass a \code{region} directly (folded into
\code{opts}), which the lab's Kopah endpoint sometimes needs.
}
\examples{
\dontrun{
df <- s3read_using_region(utils::read.csv, object = "clean/x.csv",
                          bucket = "togo-data", region = "")
}
}
