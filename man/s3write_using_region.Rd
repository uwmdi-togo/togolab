% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aws.R
\name{s3write_using_region}
\alias{s3write_using_region}
\title{Write an object to S3 via a writer function, with explicit region}
\usage{
s3write_using_region(
  FUN,
  ...,
  object,
  bucket,
  region = NULL,
  opts = NULL,
  filename = NULL
)
}
\arguments{
\item{FUN}{Writer function applied to the temp file path (e.g.
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}, \code{\link[base:readRDS]{base::saveRDS()}}). It must write to the file given as
its first argument.}

\item{...}{Additional arguments passed to \code{FUN}.}

\item{object}{Destination S3 object key (or a full \verb{s3://bucket/key} string).}

\item{bucket}{Bucket name. If missing, parsed from \code{object}.}

\item{region}{S3 region. Merged into \code{opts$region} when supplied.}

\item{opts}{Optional list of further arguments passed to
\code{aws.s3::put_object()}.}

\item{filename}{Optional fixed temp filename (otherwise a tempfile with the
object's extension is used).}
}
\value{
Invisibly, the result of \code{aws.s3::put_object()}.
}
\description{
Writes \code{x}/your data to a temp file with \code{FUN}, then uploads it with
\code{aws.s3::put_object()}. Like \code{aws.s3::s3write_using()} but lets you pass a
\code{region} directly (folded into \code{opts}).
}
\examples{
\dontrun{
s3write_using_region(ggplot2::ggsave, plot = p, object = "fig/umap.png",
                     bucket = "scrna", region = "", width = 10, height = 10)
}
}
