% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine_checkboxes.R
\name{combine_checkboxes}
\alias{combine_checkboxes}
\title{Collapse REDCap checkbox columns into a single labelled column}
\usage{
combine_checkboxes(df, base_name, levels, sep = " & ", drop = TRUE)
}
\arguments{
\item{df}{A data frame containing the \code{base___N} columns.}

\item{base_name}{Character. The checkbox field stem (e.g. "race").}

\item{levels}{Character vector of option labels, in REDCap option order
(so \code{levels[1]} corresponds to \code{base___1}, etc.).}

\item{sep}{Separator used when more than one option is checked.}

\item{drop}{If TRUE (default) the original \code{base___N} columns are removed.}
}
\value{
The data frame with a new \code{base_name} column (and, by default, the
per-option columns dropped).
}
\description{
REDCap exports a multi-select ("checkbox") field as one 0/1 column per
option, named \code{base___1}, \code{base___2}, ... This collapses them into a single
character column called \code{base}, joining the labels of every checked option
with \code{sep}. It is the R port of the Python \code{combine_checkboxes()} helper used
across the data-harmonization study scripts.
}
